<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.tps.mapper.TaskMainMapper">
    
    <resultMap type="TaskMain" id="TaskMainResult">
        <result property="taskId"    column="task_id"    />
        <result property="title"    column="title"    />
        <result property="description"    column="description"    />
        <result property="deadline"    column="deadline"    />
        <result property="priority"    column="priority"    />
        <result property="needConfirm"    column="need_confirm"    />
        <result property="status"    column="status"    />
        <result property="creatorId"    column="creator_id"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectTaskMainVo">
        select task_id, title, description, deadline, priority, need_confirm, status, creator_id, create_time, update_time from task_main
    </sql>

    <select id="selectTaskMainList" parameterType="TaskMain" resultMap="TaskMainResult">
        <include refid="selectTaskMainVo"/>
        <where>  
            <if test="title != null  and title != ''"> and title = #{title}</if>
            <if test="description != null  and description != ''"> and description = #{description}</if>
            <if test="deadline != null "> and deadline = #{deadline}</if>
            <if test="priority != null "> and priority = #{priority}</if>
            <if test="needConfirm != null "> and need_confirm = #{needConfirm}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
            <if test="creatorId != null "> and creator_id = #{creatorId}</if>
        </where>
    </select>
    
    <select id="selectTaskMainByTaskId" parameterType="String" resultMap="TaskMainResult">
        <include refid="selectTaskMainVo"/>
        where task_id = #{taskId}
    </select>

    <insert id="insertTaskMain" parameterType="TaskMain">
        insert into task_main
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="taskId != null">task_id,</if>
            <if test="title != null and title != ''">title,</if>
            <if test="description != null and description != ''">description,</if>
            <if test="deadline != null">deadline,</if>
            <if test="priority != null">priority,</if>
            <if test="needConfirm != null">need_confirm,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="creatorId != null">creator_id,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="taskId != null">#{taskId},</if>
            <if test="title != null and title != ''">#{title},</if>
            <if test="description != null and description != ''">#{description},</if>
            <if test="deadline != null">#{deadline},</if>
            <if test="priority != null">#{priority},</if>
            <if test="needConfirm != null">#{needConfirm},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="creatorId != null">#{creatorId},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateTaskMain" parameterType="TaskMain">
        update task_main
        <trim prefix="SET" suffixOverrides=",">
            <if test="title != null and title != ''">title = #{title},</if>
            <if test="description != null and description != ''">description = #{description},</if>
            <if test="deadline != null">deadline = #{deadline},</if>
            <if test="priority != null">priority = #{priority},</if>
            <if test="needConfirm != null">need_confirm = #{needConfirm},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where task_id = #{taskId}
    </update>

    <delete id="deleteTaskMainByTaskId" parameterType="String">
        delete from task_main where task_id = #{taskId}
    </delete>

    <delete id="deleteTaskMainByTaskIds" parameterType="String">
        delete from task_main where task_id in 
        <foreach item="taskId" collection="array" open="(" separator="," close=")">
            #{taskId}
        </foreach>
    </delete>
</mapper>